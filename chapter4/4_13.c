#include "stdio.h"
#include "string.h"
void reverse(char s[]);

int main(int argc, char const *argv[])
{
	
	return 0;
}

void reverse(char s[]) {
	static int i, j;
	// int i = 0;
	char t[];
	// int len = strlen(s);

	if(len--) {
		t[i] = s[i];
		i++;
		reverse(s);
	}
	else {
		
	}
}